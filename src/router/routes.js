const routes = {
  HOME: '/home',
  LOGIN: '/',
  REGISTER: '/register',
  SUSCRIPCIONES: '/suscripciones',
  NOT_FOUND: '*'
};

export const privateRoutes = [
  routes.SUSCRIPCIONES
];

export default routes;
